# Recovered Frontend Assets

These files were recovered from the original packaged TOS Electron app:

`C:\Users\Jax_shi\Desktop\win-unpacked\tms-electron-app\dist-diagnostics\win-unpacked\resources\app.asar`

The Electron build copies this directory to `dist-frontend` before packaging so
the generated `TOS.exe` keeps the original UI while the maintainable source tree
is rebuilt around it.
